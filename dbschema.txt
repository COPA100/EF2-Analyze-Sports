FIRESTORE DATABASE SCHEMA:

user
    playerId: string

gameSession
    players[string]: stores playerId's
    startTime: timestamp
    tableId: string

shots
    gameId: string
    playerId: string
    result: string ("miss" or "make")
    shotFrom: string (1-6 depending on area on the court)
